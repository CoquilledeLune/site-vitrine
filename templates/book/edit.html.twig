{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
<div class="bg-secondary mt-5 col-4 ms-5">
    <h3 class="text-light pt-2 ms-5">Modifier</h3>
        <div class="ms-5">
        {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.image) }}
        {{ form_row(form.link) }}
        {{ form_row(form.categories) }}
            <button class="btn">{{ button_label|default('Enregistrer') }}</button>
        {{ form_end(form) }}

    <a href="{{ path('app_creation_index') }}">Retour à la liste</a>

    {{ include('book/_delete_form.html.twig') }}
</div>
{% endblock %}
