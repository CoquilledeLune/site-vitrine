{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
<div class="bg-light mt-5 col-4 ms-5">
    <h3 class="text-dark pt-2">{{ book.title }}</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Couverture</th>
                <td class=""><img class="pict" src="{{ vich_uploader_asset(book) }}" alt="{{ book.title }}"></td> 
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ book.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_creation_index') }}">Retour à la liste</a>

    <a href="{{ path('app_book_edit', {'id': book.id}) }}">Modifier</a>

    {{ include('book/_delete_form.html.twig') }}
</div>
{% endblock %}
