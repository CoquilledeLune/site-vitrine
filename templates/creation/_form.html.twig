{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.size) }}
    {{ form_row(form.image) }}
    {{ form_row(form.price) }}
    {{ form_row(form.category) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
