{% extends 'base.html.twig' %}

{% block title %}Creation{% endblock %}

{% block body %}
<div class="bg-light mt-5 col-4 ms-5">
    <h3 class="text-dark pt-2">{{ creation.title }}</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ creation.description }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ creation.size }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td class=""><img class="pict" src="{{asset('build/images/' ~ creation.image)}}" 
                alt="{{ creation.image }}"></td> 
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ creation.price }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ creation.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_creation_index') }}">Retour à la liste</a>

    <a href="{{ path('app_creation_edit', {'id': creation.id}) }}">Modifier</a>

    {{ include('creation/_delete_form.html.twig') }}
</div>
{% endblock %}
